# Auto-merge gate test

Filed via Linear with `agent-trusted` label. If you see this on master, the gate evaluated, found the change in-scope + small + label-ok, ran `gh pr merge --auto --squash`, and the merge landed without human review.